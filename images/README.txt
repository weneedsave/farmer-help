图片文件夹 —— 可以放自定义头像和背景图

文件说明:
  user_avatar.png   → 用户头像 (聊天时显示在用户消息旁)
  ai_avatar.png     → AI助手头像 (聊天时显示在AI消息旁，小农)
  background.png    → 页面背景图 (可选，放这里后自动生效)
  logo.png          → 页面顶部Logo (可选)

支持格式: png, jpg, gif, webp
推荐尺寸: 头像 200x200px，背景 1920x1080px
如果不放对应图片，系统会使用默认的 🌾 emoji 作为头像。
